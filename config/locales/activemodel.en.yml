en:
  activemodel: 
    notices:
      models:
        search_result:
          create: "Upload success, please wait for the results."
    errors:
      models:
        search_result:
          attributes:
            csv_file:
              invalid_extension: "CSV file is invalid, must have .csv extension."
              invalid_count: "CSV file is invalid, must have 1-1000 keywords."
